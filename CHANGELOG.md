# Changelog

本文件记录项目对用户可见的主要变更。

## v1.1.0 - 2026-04-28

### 功能
- **打印版式**：在 **STEP4. 页组与排序** 中管理页组（可新增）、按每组 12 格/页摆放款式；目标格已有款式时弹窗确认 **对调** 位置。STEP3 仅维护数量与款号，不再提供页组/序号输入与「下一空位」等快捷按钮。
- **预览斜杠**：某尺码 **计划数量未填** 时，仅在该列「计划→实际」格内画对角线；可在备货清单编辑区 **关闭** 以便打印后手写。
- **尺码预设**：商品库增加中文名下拉，选中后自动填入对应尺码串，仍支持手改。

### 维护与发版
- 应用版本与扩展 `manifest` 同步至 `1.1.0`（与界面角标、`appVersion` 字段一致）。
- 依赖整理：移除与 `devDependencies` 重复的 `vite` 生产侧声明；移除未使用的 `@` 路径别名与无效 `tsconfig` 排除项。
- 新增 `npm run smoke:archive`，对 `parseArchiveBundle` 做无 UI 的导入兼容烟测（旧无 `schemaVersion`、当前 schema、过高 schema 拒绝、`settings` 中导出开关与 **斜杠开关** 解析）。

### 文档与行为说明
- 存档 JSON 中的 `settings`：`exportPdf` / `exportImage` / `exportJson` 仍为一键存档元数据（导入不进导出 UI）；**`strikeEmptyQtyOnPreview`** 会随导入恢复，控制「按尺码列、计划为空」时的斜杠显示。
- 备货款式可选字段 **`printSheet`**（页组）、**`printSlot`**（组内版面序号）；缺省时导入后按清单顺序自动归一（见 `ITERATION.md`）。

### 产物
- 浏览器扩展安装包：`release/fast-stocking-list-extension-v1.1.0.zip`

## v1.0.0 - 2026-04-28

### 新增与改进
- 新增导出定位稳定方案：固定截图尺寸与比例，提升 PDF 导出一致性。
- 预览页优化：当某款式未填写任何数量时，隐藏该款式总数徽标。
- 视觉风格升级为 Arc + Bento，统一导航、侧栏、表单与卡片层级。
- 作者入口移至导航标签区域（`作者：Singa`）。

### 导出与存档
- 完成导出链路回归检查（PDF、ZIP、JSON 导入导出）。
- 保持 `npm run pack` 自动清理旧产物后再打包，减少历史文件干扰。

### 清理与发布前收尾
- 清理历史外部平台模板残留配置与文案。
- 移除无用依赖 `motion`。
- 补充 README：本地使用、网站部署、浏览器扩展开发者模式使用说明。
- 扩展版本同步至 `1.0.0`。

### 产物
- 网站静态发布目录：`dist/`
- 浏览器扩展安装包：`release/fast-stocking-list-extension-v1.0.0.zip`

